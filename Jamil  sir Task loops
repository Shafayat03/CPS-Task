#include <stdio.h>

int main() {

  int n, result = 0, mod;

  printf("Enter an integer: ");
  scanf("%d", &n);

  while (n != 0) {
    mod = n % 10;
    result = result * 10 + mod;
    n /= 10;
  }

  printf("%d\n", result);

  return 0;
}
